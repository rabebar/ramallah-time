services:
  - type: web
    name: ramallah-time-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 1000 app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ramallah-time-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: MASTER_PASSWORD
        value: Ruba2025!!

databases:
  - name: ramallah-time-db
    plan: free
    region: frankfurt