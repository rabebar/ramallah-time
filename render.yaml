services:
  # 1. إعداد السيرفر (Web Service)
  - type: web
    name: ramallah-time-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 300 app:app
    envVars:
      # استخدام الرابط الخارجي المباشر لحل مشكلة الاتصال تماماً
      - key: DATABASE_URL
        value: postgresql://ramallah_time_db_eru0_user:YIfWPguYG7vZVasIqCGKuql7d4toUWIL@dpg-d783k37kijhs73fuvq7g-a.frankfurt-postgres.render.com/ramallah_time_db_eru0
      - key: SECRET_KEY
        generateValue: true
      - key: MASTER_PASSWORD
        value: Ruba2025!! # كلمة السر للوحة السوبر أدمن

databases:
  # 2. إعداد قاعدة البيانات (PostgreSQL)
  - name: ramallah-time-db
    plan: free
    region: frankfurt